extends baseEmail 

block content 
    p Hi ,
    p Welcome to Weather app, we're glad to have you 🎉🙏
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
        tbody
            tr
                td(align='left')
                    table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                        tbody
                            tr
                                td
                                    table.weather-table
                                        tbody
                                            tr
                                                td
                                                    h5= `City: ${current.location.name} (${current.location.localtime})`
                                                    p= `Wind: ${current.current.wind_kph} km/h`
                                                    p= `Humidity: ${current.current.humidity}%`
                                                    p= `Temperature: ${current.current.temp_c}°C`
    p If you need Unsubscribe notification weather updates, you can do so by clicking the link below.
    form(action='http://localhost:3000/unsubscribed-email', method='POST')
        input(type='email', name='email', placeholder='Your Email')
        button.btn.btn-primary(type='submit') Unsubscribe
